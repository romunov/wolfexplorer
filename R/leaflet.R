# Create an empty map with specific bounds.
source("./R/map_empty_with_bounds.R", local = TRUE)

# Add markers for selected animals.
source("./R/map_add_selected_animals_markers.R", local = TRUE)

# Add markers for offspring.
source("./R/map_add_offspring_markers.R", local = TRUE)

# Add markers and lines for selected offspring to map
source("./R/map_add_muerte_markers.R", local = TRUE)

# Add MCP for selected animals
source("./R/map_add_mcp.R", local = TRUE)

# Add layer controls to turn on/off layers.
source("./R/map_layer_controls.R", local = TRUE)
